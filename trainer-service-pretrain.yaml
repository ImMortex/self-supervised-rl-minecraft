apiVersion: v1
kind: Service
metadata:
  name: trainer-service-pretrain
spec:
  ports:
    - name: http-trainer-pretrain
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: training-pretrain-stcngurs
  sessionAffinity: None
  type: ClusterIP

